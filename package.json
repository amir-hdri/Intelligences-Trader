{
  "name": "kalaybot-ai",
  "private": true,
  "workspaces": [
    "robot trader",
    "robot trader/server",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace app",
    "typecheck": "npm run typecheck --workspace app",
    "test": "npm test --workspaces --if-present",
    "test:e2e": "npm run test:e2e --workspace app",
    "check": "npm run typecheck && npm run build && npm test --workspaces --if-present"
  },
  "overrides": {
    "adm-zip": "^0.6.0",
    "brace-expansion@^1.0.0": "^1.1.18",
    "brace-expansion@^2.0.0": "^2.1.4",
    "ip-address": "^10.4.0",
    "js-yaml": "^4.3.1",
    "qs": "^6.15.2",
    "tar": "^7.5.22",
    "undici": "^7.29.0",
    "@mapbox/node-pre-gyp": "^2.0.3"
  }
}
